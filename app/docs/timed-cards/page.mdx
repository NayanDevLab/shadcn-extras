import { ComponentPreview } from '@/components/website/component-preview';

export const metadata = {
  title: 'Timed Cards Gallery',
  description: 'A cinematic, timed cards gallery with smooth GSAP animations.',
};

# Timed Cards

A cinematic, full-screen timed cards gallery with smooth sequence animations built with GSAP. It features automatic progression, custom layouts, and a sleek presentation ideal for hero sections or landing pages.

## Basic Layout

<ComponentPreview name="timed-cards-basic" />

## Installation

This component depends on `gsap` for complex sequence animations.

```bash
npm install gsap lucide-react
```

<br />

<br />

Copy and paste the following code into your project.

<br />
<br />

<ComponentCodePreview name="timed-cards" />
